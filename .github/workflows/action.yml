name: "scrapper action"
description: "scrap data from LGU website"
author: "zain-ul-din"

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: npx yarn
      - run: npx yarn dev
      - run: 
