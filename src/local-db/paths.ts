/**
 * Path to the main database directory used in production.
 */
export const DB_PATH = process.cwd() + "/db";

/**
 * Path to the local database directory used in development.
 */
export const LOCAL_DB_PATH = process.cwd() + "/local_db";
